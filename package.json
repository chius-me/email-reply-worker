{
  "name": "email-reply-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@types/node": "^25.6.2",
    "typescript": "^5.5.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.136.2"
  },
  "dependencies": {
    "mimetext": "^3.0.28"
  },
  "overrides": {
    "sharp": "0.35.4"
  }
}
